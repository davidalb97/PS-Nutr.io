<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/itemCardView"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="4dp"
    android:elevation="10dp"
    app:cardCornerRadius="15dp">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="131dp"
        android:layout_margin="4dp">

        <ImageView
            android:id="@+id/recyclerItemImage"
            android:layout_width="128dp"
            android:layout_height="match_parent"
            android:padding="2dp" />

        <TextView
            android:id="@+id/recyclerItemName"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentTop="true"
            android:layout_marginTop="1dp"
            android:layout_toEndOf="@+id/recyclerItemImage"
            android:text="@string/recyclerItemName"
            android:textColor="@android:color/black"
            android:textSize="20sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/ItemDetails"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@+id/recyclerItemName"
            android:layout_toEndOf="@+id/recyclerItemImage"
            android:text="@string/recyclerItemDetails"
            android:textSize="15sp" />

    </RelativeLayout>

</androidx.cardview.widget.CardView>