package pt.ipl.isel.leic.ps.androidclient.data.db.mapper

import pt.ipl.isel.leic.ps.androidclient.data.db.entity.DbFavoriteMeal
import pt.ipl.isel.leic.ps.androidclient.data.db.entity.DbMeal
import pt.ipl.isel.leic.ps.androidclient.data.model.FavoriteMeal

// TODO - add ingredients
class FavoriteMealMapper {

   /* val ingredientMapper = IngredientMapper()

    fun mapToModel(dto: DbFavoriteMeal): FavoriteMeal =
        FavoriteMeal(
            dto.mealId,
            ingredientMapper
                .mapToListModel(dto.ingredients)
        )

    fun mapToListModel(dtos: List<DbFavoriteMeal>): List<FavoriteMeal> =
        dtos.map { mapToModel(it) }*/
}